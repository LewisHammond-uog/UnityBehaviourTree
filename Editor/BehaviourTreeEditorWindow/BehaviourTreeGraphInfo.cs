using UnityEngine;

//Struct to store the info needed by the a Behaviour Tree Node to display it properly in the graph
public struct BehaviourTreeGraphInfo
{
    public string guid;
    public Vector2 position;
}
